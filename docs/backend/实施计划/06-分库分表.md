# 阶段 6：分库分表

## 目标

实现按 `space_id` 分表（ShardingSphere 自定义分片 + 动态建表）。

## 前置依赖

阶段 4（图片上传）。

## 任务清单

- [x] ShardingSphere 分片规则配置
- [x] `PictureShardingAlgorithm` 自定义分片算法
- [x] `DynamicShardingManager` 动态建表 + 规则刷新
- [x] 按 `space_id` 路由联调验证

## 关键类

- `PictureShardingAlgorithm`
- `DynamicShardingManager`

## 参考文档

- [详细设计文档](../详细设计文档.md)（分表路由流程）
- [数据库设计文档](../数据库设计文档.md)（分库分表说明）

## 验收标准

- `space_id` 为空落到 `picture`，非空落到 `picture_{space_id}`
- 团队空间创建时自动建表
- 查询/写入正确路由到对应分表
